# Core dependencies (required)
numpy>=1.20.0
tqdm>=4.60.0

# I/O dependencies (at least one recommended)
h5py>=3.0.0        # Preferred for HDF5 support
# tensorflow>=2.0.0  # Alternative for HDF5 support (heavier)

# Optional GPU acceleration (install one for CUDA support)
# cupy-cuda11x>=10.0.0  # For CUDA 11.x
# cupy-cuda12x>=12.0.0  # For CUDA 12.x

# GUI dependencies (for mnist_gui.py)
# tkinter is usually included with Python installation

# Development/testing (optional)
# pytest>=6.0.0
# black>=21.0.0
# flake8>=3.8.0